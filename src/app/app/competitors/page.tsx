import { EmptyState } from "@/components/EmptyState";

export default function CompetitorsPage() {
  return <EmptyState title="Competitors" sentence="Mentions of the competitors you track appear here once a scan has run." />;
}
