import { EmptyState } from "@/components/EmptyState";

export default function LeadsPage() {
  return <EmptyState title="Leads" sentence="Scored Reddit posts and comments appear here once a scan has run." />;
}
