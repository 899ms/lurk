import { EmptyState } from "@/components/EmptyState";

export default function InsightsPage() {
  return <EmptyState title="Insights" sentence="Pain themes and community stats appear here once there are leads to cluster." />;
}
